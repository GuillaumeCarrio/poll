import { IsCreatorDirective } from './is-creator.directive';

describe('IsCreatorDirective', () => {
  it('should create an instance', () => {
    const directive = new IsCreatorDirective();
    expect(directive).toBeTruthy();
  });
});
